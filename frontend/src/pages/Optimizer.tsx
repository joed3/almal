export default function Optimizer() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold text-white mb-3">Optimizer</h1>
      <p className="text-gray-400">
        Run quantitative portfolio optimization models — mean-variance, risk
        parity, and more — to generate recommended weight allocations.
      </p>
    </div>
  );
}
